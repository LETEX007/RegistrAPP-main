import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.registrapp',
  appName: 'RegistrAPP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
